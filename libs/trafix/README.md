# trafix

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build trafix` to build the library.

## Running unit tests

Run `nx test trafix` to execute the unit tests via [Jest](https://jestjs.io).
